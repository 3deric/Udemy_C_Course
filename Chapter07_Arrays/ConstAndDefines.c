#include <stdio.h>

#define NUMBER 4

int main()
{
    // const int num = 4; doesnt work on all systems
    // use defines to declare constants
    // the compiler replaces the defines with the magic number

    int numbers[NUMBER];


}
